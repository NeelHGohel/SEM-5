import '../utils/import_export.dart';

class ExceptionSnackbar extends StatelessWidget {
  const ExceptionSnackbar({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Error SnakeBar")));
  }
}
